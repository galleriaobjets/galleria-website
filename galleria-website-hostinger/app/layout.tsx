
import './globals.css'
export const metadata={title:'Galleria Objets — Live',description:'AI-driven, Hostinger DB-backed site.'}
export default function RootLayout({children}:{children:React.ReactNode}){
  return <html lang="en"><body>{children}</body></html>
}
